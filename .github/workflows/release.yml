name: Release (Docker & NPM)

on:
  push:
    tags: [ 'v*' ]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}
  VERSION: ${{ github.ref_name }} # e.g. v0.2.0-alpha.3

jobs:
  build-wasm:
    name: Build WASM SDK
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown

      - name: Sync version from tag
        run: |
          # Remove 'v' prefix if present
          VER=$(echo "${{ env.VERSION }}" | sed 's/^v//')
          echo "Syncing version to $VER"
          # Root Cargo.toml
          sed -i "s/^version = \".*\"/version = \"$VER\"/" Cargo.toml
          # Only published package.json
          sed -i "s/\"version\": \".*\"/\"version\": \"$VER\"/" crates/hot/package.json

      - name: Install wasm-pack
        run: curl https://rustwasm.github.io/wasm-pack/installer/init.sh -sSf | sh

      - name: Build WASM (@hotpaxel/hot)
        run: wasm-pack build --target web --release --scope hotpaxel
        working-directory: crates/hot

      - name: Upload @hotpaxel/hot package artifact
        uses: actions/upload-artifact@v6
        with:
          name: hot
          path: crates/hot/pkg/

  build-editor:
    name: Build Editor UI
    needs: build-wasm
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Download @hotpaxel/hot package
        uses: actions/download-artifact@v6
        with:
          name: hot
          path: crates/hot/pkg

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: Build hot-editor
        run: bun run build
        working-directory: apps/hot-editor

      - name: Generate API docs
        run: bun run gen:docs

      - name: Upload @hotpaxel/hot-editor artifact
        uses: actions/upload-artifact@v6
        with:
          name: hot-editor
          path: apps/hot-editor/dist/

      - name: Upload docs artifact
        uses: actions/upload-artifact@v6
        with:
          name: docs
          path: apps/docs/

  build-backend:
    name: Build Backend (${{ matrix.arch }})
    strategy:
      matrix:
        arch: [amd64, arm64]
        include:
          - arch: amd64
            target: x86_64-unknown-linux-gnu
          - arch: arm64
            target: aarch64-unknown-linux-gnu
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Sync version from tag
        run: |
          VER=$(echo "${{ env.VERSION }}" | sed 's/^v//')
          sed -i "s/^version = \".*\"/version = \"$VER\"/" Cargo.toml
          sed -i "s/\"version\": \".*\"/\"version\": \"$VER\"/" crates/hot/package.json

      - name: Setup Zig
        uses: mlugg/setup-zig@v2

      - name: Install cargo-zigbuild
        uses: taiki-e/install-action@v2
        with:
          tool: cargo-zigbuild

      - name: Install dependencies
        run: sudo apt-get update && sudo apt-get install -y pkg-config libssl-dev protobuf-compiler

      - name: Build backend
        run: cargo zigbuild --release --target ${{ matrix.target }}
        working-directory: crates/paxel

      - name: Upload backend artifact
        uses: actions/upload-artifact@v6
        with:
          name: paxel-${{ matrix.arch }}
          path: target/${{ matrix.target }}/release/paxel

  npm-publish:
    name: NPM Publish
    needs: build-wasm
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write # Required for NPM OIDC provenance
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Download @hotpaxel/hot package
        uses: actions/download-artifact@v6
        with:
          name: hot
          path: crates/hot/pkg

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '24'

      - name: Determine NPM Tag
        id: npm-tag
        run: |
          if [[ "${{ env.VERSION }}" == *"alpha"* ]]; then
            echo "tag=alpha" >> "$GITHUB_OUTPUT"
          elif [[ "${{ env.VERSION }}" == *"beta"* ]]; then
            echo "tag=beta" >> "$GITHUB_OUTPUT"
          else
            echo "tag=latest" >> "$GITHUB_OUTPUT"
          fi

      - name: Publish to NPM (@hotpaxel/hot)
        run: |
          PKG_NAME=$(node -p "require('./package.json').name")
          PKG_VER=$(node -p "require('./package.json').version")

          if npm view "${PKG_NAME}@${PKG_VER}" version >/dev/null 2>&1; then
            echo "🔍 Version ${PKG_VER} already exists on NPM. Verifying package content integrity..."

            TMP_DIR=$(mktemp -d)
            trap "rm -rf \"$TMP_DIR\"" EXIT
            mkdir -p "$TMP_DIR/local" "$TMP_DIR/remote" "$TMP_DIR/local-pkg" "$TMP_DIR/remote-pkg"

            # 1. Package both remote and local tarballs
            REMOTE_TAR=$(npm pack "${PKG_NAME}@${PKG_VER}" --pack-destination "$TMP_DIR/remote" 2>/dev/null | tail -n 1)
            LOCAL_TAR=$(npm pack --pack-destination "$TMP_DIR/local" 2>/dev/null | tail -n 1)

            # 2. Extract tarballs
            tar -xzf "$TMP_DIR/local/$LOCAL_TAR" -C "$TMP_DIR/local-pkg"
            tar -xzf "$TMP_DIR/remote/$REMOTE_TAR" -C "$TMP_DIR/remote-pkg"

            # 3. Byte-level comparison of pure package contents
            if git diff --no-index --quiet "$TMP_DIR/remote-pkg/package" "$TMP_DIR/local-pkg/package"; then
              echo "✅ Content matches published package perfectly. Skipping publish."
              exit 0
            else
              echo "❌ [FATAL] Content mismatch detected for ${PKG_NAME}@${PKG_VER}!"
              echo "The version is already published to NPM with DIFFERENT contents."
              echo "Please bump the version (semver) to publish new changes."
              git diff --no-index --stat "$TMP_DIR/remote-pkg/package" "$TMP_DIR/local-pkg/package"
              exit 1
            fi
          else
            echo "🚀 Publishing ${PKG_NAME}@${PKG_VER} to NPM..."
            npm publish --access public --tag ${{ steps.npm-tag.outputs.tag }}
          fi
        working-directory: crates/hot/pkg

  docker-publish:
    name: Docker Publish (Hotpaxel - Unified)
    needs: [build-editor, build-backend]
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Download amd64 backend artifact
        uses: actions/download-artifact@v6
        with:
          name: paxel-amd64
          path: artifacts/amd64

      - name: Download arm64 backend artifact
        uses: actions/download-artifact@v6
        with:
          name: paxel-arm64
          path: artifacts/arm64

      - name: Download @hotpaxel/hot-editor artifact
        uses: actions/download-artifact@v6
        with:
          name: hot-editor
          path: public

      - name: Download docs artifact
        uses: actions/download-artifact@v6
        with:
          name: docs
          path: docs

      - name: Log in to the Container registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ github.repository_owner }}/hotpaxel
          tags: |
            type=ref,event=tag
            type=raw,value=latest,enable=${{ !contains(github.ref_name, 'alpha') && !contains(github.ref_name, 'beta') }}

      - name: Build and push Hotpaxel (Unified)
        uses: docker/build-push-action@v6
        with:
          context: .
          file: .docker/release.Dockerfile
          push: true
          platforms: linux/amd64,linux/arm64
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max


  github-release:
    name: Create GitHub Release
    needs: [build-wasm, build-editor, build-backend, npm-publish, docker-publish]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Download paxel-amd64
        uses: actions/download-artifact@v6
        with:
          name: paxel-amd64
          path: release-artifacts/paxel-amd64

      - name: Download paxel-arm64
        uses: actions/download-artifact@v6
        with:
          name: paxel-arm64
          path: release-artifacts/paxel-arm64

      - name: Download hot-editor
        uses: actions/download-artifact@v6
        with:
          name: hot-editor
          path: release-artifacts/hot-editor

      - name: Prepare release artifacts (ZIP)
        run: |
          # Use internal filenames as original 'paxel', but zip with descriptive names
          cd release-artifacts/paxel-amd64 && zip ../../paxel-linux-amd64-${{ github.ref_name }}.zip paxel && cd ../..
          cd release-artifacts/paxel-arm64 && zip ../../paxel-linux-arm64-${{ github.ref_name }}.zip paxel && cd ../..
          cd release-artifacts/hot-editor && zip -r ../../hot-editor-${{ github.ref_name }}.zip ./* && cd ../..

      - name: Generate release notes
        id: generate_notes
        run: |
          # Fetch auto-generated notes from GitHub API
          # This will use the categories defined in .github/release.yml
          API_URL="https://api.github.com/repos/${{ github.repository }}/releases/generate-notes"
          RESPONSE=$(curl -s -X POST \
            -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
            -H "Accept: application/vnd.github.v3+json" \
            $API_URL \
            -d "{\"tag_name\":\"${{ github.ref_name }}\"}")
          
          NOTES=$(echo "$RESPONSE" | jq -r '.body')
          
          # Prepare deployment footer
          FOOTER="
          ## 🚀 Deployment Options
          
          ### 📦 All-in-One (Unified)
          Full-stack solution with Backend and Frontend integrated in a single container.
          - 🐳 **Docker Image:** [ghcr.io/${{ github.repository_owner }}/hotpaxel:${{ github.ref_name }}](https://github.com/${{ github.repository }}/pkgs/container/hotpaxel)
          
          ### 🏗️ Standalone API (Server Only)
          Disable UI using `DISABLE_UI=true` environment variable or `--disable-ui` flag.
          - 🐳 **Docker Image:** [ghcr.io/${{ github.repository_owner }}/hotpaxel:${{ github.ref_name }}](https://github.com/${{ github.repository }}/pkgs/container/hotpaxel)
          - 💾 **Binaries:** See the **Assets** section below for platform-specific ZIP files.
          
          ### 📚 Library & SDK
          - 📦 **NPM Package:** [@hotpaxel/hot](https://www.npmjs.com/package/@hotpaxel/hot)
          "
          
          # Combine Auto Notes + Custom Footer
          FINAL_BODY="${NOTES}${FOOTER}"
          
          # Escape for GitHub Actions output
          {
            echo "body<<EOF"
            echo "$FINAL_BODY"
            echo "EOF"
          } >> "$GITHUB_OUTPUT"

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          body: ${{ steps.generate_notes.outputs.body }}
          files: |
            paxel-linux-amd64-${{ github.ref_name }}.zip
            paxel-linux-arm64-${{ github.ref_name }}.zip
            hot-editor-${{ github.ref_name }}.zip
          tag_name: ${{ github.ref_name }}
          draft: false
          prerelease: contains(github.ref_name, 'alpha') || contains(github.ref_name, 'beta')
