{
  "name": "hotpaxel",
  "private": true,
  "packageManager": "bun@1.1.0",
  "workspaces": [
    "crates/*",
    "apps/*"
  ],
  "scripts": {
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "clean": "turbo run clean && rm -rf .turbo",
    "purge": "bun run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}