{
  "name": "hotpaxel",
  "private": true,
  "packageManager": "bun@1.1.0",
  "workspaces": [
    "crates/*",
    "apps/*"
  ],
  "scripts": {
    "build": "bunx turbo build",
    "lint": "bunx turbo lint",
    "test": "bunx turbo test",
    "clean": "cargo clean && bunx turbo run clean && rm -rf .turbo",
    "purge": "bun run clean && find . -name \"node_modules\" -type d -prune -exec rm -rf '{}' +"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}