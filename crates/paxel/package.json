{
    "name": "@hotpaxel/paxel",
    "private": true,
    "scripts": {
        "build": "cargo build --release",
        "dev": "cargo run",
        "test": "cargo test",
        "clean": "cargo clean && rm -rf .turbo"
    }
}